<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ehsanrc.boipoka">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".controllers.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controllers.CategorizedBooks" />
        <activity
            android:name=".controllers.Chapters"
            android:label="@string/title_activity_chapters"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".controllers.ChapterDetail"
            android:label="@string/title_activity_chapter_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".controllers.BookInfo" />
        <activity android:name=".controllers.AboutBook" />
        <activity android:name=".controllers.Reviews" />
        <activity android:name=".controllers.AddReview" />
        <activity android:name=".controllers.Favorites" />
        <activity android:name=".controllers.ToRead" />
        <activity android:name=".controllers.Reading" />
        <activity
            android:name=".controllers.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".controllers.Register"></activity>
    </application>

</manifest>
